apiVersion: camel.apache.org/v1
kind: Pipe
metadata:
  name: my-bind
spec:
  steps:
    - ref:
        kind: Kamelet
        apiVersion: camel.apache.org/v1
        name: extract-field-action
      properties:
        <caret>
